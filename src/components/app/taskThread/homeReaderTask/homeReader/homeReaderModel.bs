namespace Models

    class HomeReaderModel extends Model

        state = {
            loading: false,
            contentNode: Rotor.Utils.createContentNode({
                state: "empty" ' this is not in use, but looks good in the model
            })
        }

    end class

end namespace
